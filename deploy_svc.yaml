apiVersion: v1
kind: Service
metadata:
  name: deploy-svc
spec:
  ports:
  - protocol: TCP
    name: nginx
    port: 80
    targetPort: 80
  - protocol: TCP
    name: multitool
    port: 8080
    targetPort: 1180
  selector:
    app: nginx-multitool
